<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true"
    android:background="#FFFFFF">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="32dp"
        android:gravity="center_horizontal">

        <!-- Espaço flexível superior para centralização perfeita -->
        <View
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1" />

        <!-- 🌲 Logotipo do Recicla Fácil (Padrão das outras telas) -->
        <ImageView
            android:id="@+id/ivForgotLogo"
            android:layout_width="160dp"
            android:layout_height="160dp"
            android:src="@drawable/logo_recicla"
            android:contentDescription="Logo Recicla Fácil"
            android:layout_marginBottom="24dp" />

        <!-- Texto de Instrução Amigável -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Digite o seu e-mail cadastrado abaixo para receber as instruções de recuperação de senha."
            android:textSize="15sp"
            android:textColor="#657786"
            android:gravity="center"
            android:layout_marginBottom="40dp"
            android:lineSpacingMultiplier="1.2" />

        <!-- ✉️ Campo E-mail -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="E-mail"
            android:textColor="#14171A"
            android:textStyle="bold"
            android:textSize="14sp"
            android:layout_marginBottom="8dp" />

        <EditText
            android:id="@+id/etForgotEmail"
            android:layout_width="match_parent"
            android:layout_height="54dp"
            android:hint="exemplo@email.com"
            android:inputType="textEmailAddress"
            android:background="@drawable/edit_text_background"
            android:paddingHorizontal="16dp"
            android:textColor="#14171A"
            android:textColorHint="#AAB8C2"
            android:layout_marginBottom="32dp"
            android:textSize="16sp" />

        <!-- 🚀 Botão Enviar Instruções -->
        <Button
            android:id="@+id/btnRecoverPassword"
            android:layout_width="match_parent"
            android:layout_height="54dp"
            android:text="Enviar Instruções"
            android:backgroundTint="@color/app_green"
            android:textColor="#FFFFFF"
            android:textSize="16sp"
            android:textStyle="bold"
            android:insetTop="0dp"
            android:insetBottom="0dp"
            app:cornerRadius="12dp"
            android:layout_marginBottom="24dp"
            android:elevation="2dp" />

        <!-- Botão para Voltar ao Login -->
        <TextView
            android:id="@+id/tvBackToLoginFromForgot"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Voltar para o Login"
            android:textColor="@color/app_green"
            android:textStyle="bold"
            android:textSize="15sp"
            android:padding="8dp" />

        <!-- Espaço flexível inferior -->
        <View
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1" />

    </LinearLayout>
</ScrollView>